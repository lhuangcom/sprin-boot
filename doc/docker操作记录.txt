配置永久DNS：打开文件 /etc/network/interfaces  配置dns-nameservers 8.8.8.8

没有已启用的仓库。 执行 "yum repolist all" 查看您拥有的仓库。
centos中使用才可以，我这里使用的是Ubuntu，ubuntu默认软件包管理器不是yum，而是dpkg，安装软件时用apt-get你说的网上常见法解决方法，命令改为代码:sudo apt-get install net-tools，这样就安装好了。

Ubuntu出现“E: 无法定位软件包问题”解决方法
sudo apt-get update

风烛残年之躯


网关：
配置路由：zuul.routes.(自定义):
                 path:自定义路径配置
                 serviceId:项目名


简洁写法：zuul.routes.（serviceId:）：（path）
排除路由：zuul:ignored-patterns: （set集合路径）
配置cookie：zuul.routes.sensitiveHeaders = 


实现动态配置：@RefreshScope
Circuit Breaker 断路器


zuul超时：第一次访问由于懒加载过程，且hystrix默认超时时长较短，容易触发超时，需要修改hystrix的默认超时时长


网易蜂巢镜像中心
https://c.163.com/hub#/m/search
win10 https://www.docker.com/products/docker#/windows  

sudo su root

apt-get update 更新apt-get

强制解锁命令：sudo rm /var/cache/apt/archives/lock
sudo rm /var/lib/dpkg/lock

apt-get install -y docker.io
最新版：curl -s https://get.docker.com|sh
启动docker ： service docker start


拉取镜像：docker pull [OPTIONS] NAME[:TAG]
查看镜像 docker images [OPTIONS] [REPOSITORY[:TAG]]

配置国内镜像地址：
{
  "registry-mirrors": ["https://fy707np5.mirror.aliyuncs.com"]
}
systemctl daemon-reload
systemctl restart docker

运行镜像
docker run [OPTIONS] IMAGE[:TAG] [COMMAND] [ARG...]

查看目前运行的容器 ：docker ps
docker运行Nginx:
 后台运行：docker run -d IMAGE[:TAG] [COMMAND] [ARG...]
 查看容器内部：docker exec -it  容器id bash
  查看Nginx位置：which nginx

docker ps报错bash: ps: command not found 
解决：apt-get update && apt-get install procps

                宿主机端口 ：容器内部端口
dockerrun -d -p 8080:80  IMAGE[:TAG] [COMMAND] [ARG...]

netstat -na|grep 8080 查看占用端口程序

