xmlns:th="http://www.thymeleaf.org"

#spring.devtools.restart.enabled=true




20181115
访问静态资源（路径）问题
20181117
完成前端页面模板设计，建立数据库
20181118
实现数据库的数据交互；
20181119
表关联：user：无关联；
             article：与content表单向一对一（没有必要取出content表数据），category（多对多，添加category集合），keyword（多对多，添加keyword集合）；
             content（独立id，存储数据库后返回id供artice表插入）；
             category：（article通过category条件查询获取相关的article）；
             keyword：（article通过key条件查询获取相关的article）；
             artcount：（artcount通过articleid条件查询获取相关的article）；

springboot使用lombok；
映射；表关联；mybati级联：mybatis使用Resource

20181120
mybatis嵌套查询（fetchType）和嵌套结果；内连接、外连接；
注意，如果查询出来的列同名，例如tb_user表的id和tb_order表的id都是id，同名。则需要使用别名区分（sql语句使用as修改，<collections>中column对应字段也要使用别名）

考虑数据冗余问题和表连接之间的性能平衡；

20181124
关闭thymeleaf的变量检测 inspections;thymeleaf处理日期格式；
20181125
首页附件功能、系统日志功能、每天打印日志功能缺失；


20181202
后期增加订阅功能、通知；

1、如果同一个目录下，有application.yml也有application.properties，默认先读取application.properties。
2、如果同一个配置属性，在多个配置文件都配置了，默认使用第1个读取到的，后面读取的不覆盖前面读取到的。

spring-boot-autoconfigure2.1.0.RELEASE\spring-configuration-metadata.json查找最新的文件配置

上传文件用的插件是Dropzone.js;
动态生成的文件放在默认的配置静态文件的路径，不能立刻被读取到；
测试更换静态资源访问路径；

富文本编辑器 summernote

20181205
防止SQL注入
ajax访问的配置


20181209
下拉框组件用的是vue-select;
使用的时间插件：moment.js
<blockquote>：标签内的内容会向后缩进4个空格
<code>:表示计算机源代码;

20181212
dto拷贝
js引用vue的相关组件，不用import；
搜索功能
增加status字段
20181214
编写covertDTO接口
20181215
form表单的butto按钮如果没有定义type=button，可能默认为submit，点击响应后自动提交表单；
搜索功能，时间格式化，更新文章功能；

20181216
增加文档注释；
切换按钮使用的是jquery-toggles

20181219
优化非空判断；
添加事务

20190106
使用websocket完成简单的聊天室功能；

20190220
完成SPI模式设计代码·

20190222
redis封装

20190228
shiro remember me 、Ehcache缓存、会话管理、并发登录控制

20190303
shiro过滤器user和authc的区别（启用记住我功能用user）；

20190304
springaop配置事务
加密算法

20190305
任务调度,文件上传（post请求提交的数据默认放在请求body中，并非提交json数据）

20190308
文件下载功能；
bootstrapTable（表信息根据搜索查询，要注意返回的数据格式）


自动插入单个建议：Ctrl + 空格
最近文件：Ctrl E


20190327
如果shiro管理session，shiro会不兼容request中的session；（测试验证码的session）

20190701
springboot完成session共享
注意：（springboot1.5.6）引入spring-session-data-redis后配置文件需为properties格式文件，因为yml文件配置的信息无法被读取；（2.0.2版本解决问题）


20190708
完成springboot的参数校验；

20190725
完成多个数据源配置实现读写分离

Objects工具类


loading

Arthas（排查Spring Boot应用404/401问题）
spring boot优雅关闭应用
spring事件机制
7 分钟实现 Java 发送短信功能
不就是个短信登录API嘛，有这么复杂吗？
优雅地生成那些花里胡哨的复杂样式 Excel 文件
设计 jar 包
RestTemplate 实现大文件下载

在 MyBatis 里，如何将 JSON 型字段到 Java 类的映射
使用Mybatis的拦截器实现数据加密与解密

设计短url
抛开Spring来说，如何自己实现Spring AOP?
Docker 部署 Spring Cloud 项目详细步骤
统一异常处理介绍及实战
spring自定义注解
redis的发布订阅实现异步消息系统；
Logback配置文件
Java线程池异常处理的正确姿势
Lombok的使用

参数校验文档

防止xss脚本攻击
threadLocal透传
Spring Boot项目模板（秒杀项目）
秒杀场景方案设计

Spring MVC+Spring+Mybatis实现支付宝支付功能

WebMvcAutoConfiguration MVC自动化配置类
微信扫码登录